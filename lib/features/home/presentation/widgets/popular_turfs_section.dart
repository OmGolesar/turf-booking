import 'package:flutter/material.dart';

/// Popular turfs section.
class PopularTurfsSection extends StatelessWidget {
  const PopularTurfsSection({super.key});
  @override
  Widget build(BuildContext context) => const SizedBox.shrink();
}
