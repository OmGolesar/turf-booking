import 'package:flutter/material.dart';

/// Search header widget for home screen.
class SearchHeader extends StatelessWidget {
  const SearchHeader({super.key});
  @override
  Widget build(BuildContext context) => const SizedBox.shrink();
}
