import 'package:flutter/material.dart';

/// Nearby turfs horizontal list section.
class NearbyTurfsSection extends StatelessWidget {
  const NearbyTurfsSection({super.key});
  @override
  Widget build(BuildContext context) => const SizedBox.shrink();
}
