import 'package:flutter/material.dart';

/// Turf listing screen with infinite scroll, filters, and sorting.
class TurfListingScreen extends StatelessWidget {
  const TurfListingScreen({super.key});
  @override
  Widget build(BuildContext context) => const Scaffold(body: Center(child: Text('Turf Listing')));
}
